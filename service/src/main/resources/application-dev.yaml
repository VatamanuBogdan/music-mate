spring:
  banner:
    location: classpath:logo-banner.txt
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
logging:
  threshold:
    console: debug